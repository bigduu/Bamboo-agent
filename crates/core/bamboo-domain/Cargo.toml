[package]
name = "bamboo-domain"
version.workspace = true
edition = "2021"
rust-version.workspace = true
description = "Domain models and shared types for the Bamboo agent framework"
license = "MIT"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
serde_yaml = "0.9"
chrono = { workspace = true }
uuid = { workspace = true }
thiserror = { workspace = true }
regex = "1"
async-trait = "0.1"
tracing = "0.1"

[features]
test-utils = []
